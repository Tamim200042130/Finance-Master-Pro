name: build_upload_notify

on:
  push:
    branches:
      - main
      - edit

jobs:
  build_upload_notify:
    name: Build, Upload, and Notify
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Java
        uses: actions/setup-java@v1
        with:
          java-version: '12.x'

      - name: Set up Flutter
        uses: subosito/flutter-action@v1
        with:
          channel: 'stable'

      - name: Get Branch Name
        id: branch
        run: echo "branch=${GITHUB_REF##*/}" >> $GITHUB_OUTPUT

      - name: Get Today's Date
        id: date
        run: echo "date=$(date +'%d%m%y')" >> $GITHUB_OUTPUT

      - name: Read Current Version
        id: read_version
        run: echo "version=$(cat version.txt)" >> $GITHUB_OUTPUT

      - name: Increment Version
        id: increment_version
        run: |
          current_version="${{ steps.read_version.outputs.version }}"
          IFS='.' read -r -a version_parts <<< "$current_version"
          major=${version_parts[0]}
          minor=${version_parts[1]}
          
          if [ $minor -eq 9 ]; then
            major=$((major + 1))
            minor=0
          else
            minor=$((minor + 1))
          fi
          
          new_version="$major.$minor"
          echo "new_version=$new_version" >> $GITHUB_OUTPUT
          echo $new_version > version.txt

      - name: Install Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install Google API Client
        run: pip install --upgrade google-api-python-client google-auth-httplib2 google-auth-oauthlib

      - name: Install requests
        run: pip install requests

      - name: Delete Existing Files from Google Drive
        env:
          FOLDER_ID: ${{ secrets.DRIVEFOLDERID }}
          GOOGLE_CREDENTIALS_BASE64: ${{ secrets.CREDENTIALS }}
        run: python delete_drive_files.py

      - name: Install Flutter Dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Rename APK File
        run: |
          mv build/app/outputs/apk/release/app-release.apk build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk


      - name: Archive Files
        run: |
          sudo apt-get update
          sudo apt-get install -y zip
          zip -r archive.zip build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk

      - name: Upload to Google Drive
        id: upload
        uses: adityak74/google-drive-upload-git-action@main
        with:
          credentials: ${{ secrets.CREDENTIALS }}
          filename: "build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk"
          folderId: ${{ secrets.DRIVEFOLDERID }}
          name: "Finance Master Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk"
          overwrite: "true"
          mimeType: "application/vnd.android.package-archive"

      - name: Configure Git
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"

      - name: Commit Updated Version
        run: |
          git add version.txt
          git commit -m "Increment version to ${{ steps.increment_version.outputs.new_version }}"
          git push origin HEAD:${{ github.ref }}


      - name: Push to Releases
        uses: ncipollo/release-action@v1
        with:
          artifacts: "build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk,build/ios/iphoneos/app.ipa"
          tag: v${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}
          token: ${{ secrets.GH_PAT }}


      - name: Upload APK to Slack
        run: |
          curl -F file=@build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk \
               -F "initial_comment=New version of Finance Master Pro is available!" \
               -F channels=#finance-master-pro \
               -H "Authorization: Bearer ${{ secrets.SLACK_TOKEN }}" \
               https://slack.com/api/files.upload
        env:
          SLACK_TOKEN: ${{ secrets.SLACK_TOKEN }}


      - name: Notify on Slack with APK
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_USERNAME: Mohammad Tamim Ibn Hossain
          SLACK_TITLE: "🎉🎉 New Release Available 🎉🎉"
          SLACK_MESSAGE: |
            A new release of Finance Master Pro is available. Version: ${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}
          SLACK_COLOR: good
          SLACK_CHANNEL: "#finance-master-pro"
          SLACK_ICON: "https://encrypted-tbn3.gstatic.com/images?q=tbn:ANd9GcQIdeZ1MahCe2snsur1xczoHnlPx6ENMuJH6UDWWhNYjIfAQyuj"


      - name: Upload APK and Send Telegram Message
        run: python send_telegram_message.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_THREAD_ID: ${{ secrets.TELEGRAM_THREAD_ID }}
          MESSAGE: "New Version of Finance Master Pro is available. Download Now: [Download the APK](https://drive.google.com/drive/folders/1k51cCnURx-V04zzyrerRMDSo-YCWrKEP?usp=sharing)"
          APK_FILE_PATH: build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk

#      - name: Upload APK to Firebase App Distribution
#        uses: wzieba/Firebase-Distribution-Github-Action@v1.5
#        with:
#          appId: ${{ secrets.FIREBASE_APP_ID }}
#          token: ${{ secrets.FIREBASE_TOKEN }}
#          groups: "finance_master_pro"
#          file: "build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk"
#          releaseNotes: "New Version of Finance Master Pro is available. Download Now: [Download the APK](https://drive.google.com/drive/folders/1k51cCnURx-V04zzyrerRMDSo-YCWrKEP?usp=sharing)"
#          testers: "finance_master_pro"
#          notifyTesters: "true"
#          debug: "true"
#
#      - name: Upload APK to AppCenter
#        uses: wzieba/AppCenter-Upload@v2
#        with:
#          ownerName: "tamimibnhossain"
#          appName: "Finance-Master-Pro"
#          token: ${{ secrets.APPCENTER_TOKEN }}
#          file: "build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk"
#          group: "Collaborators"
#          releaseNotes: "New Version of Finance Master Pro is available. Download Now: [Download the APK](https://drive.google.com/drive/folders/1k51cCnURx-V04zzyrerRMDSo-YCWrKEP?usp=sharing)"
#          destinationType: "groups"
#          destinationName: "Collaborators"
#          notifyTesters: "true"
#          debug: "true"
#
#      - name: Upload APK to GitHub Releases
#        uses: ncipollo/release-action@v1
#        with:
#          artifacts: "build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk"
#          tag: v${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}
#          token: ${{ secrets.GH_PAT }}
#
#      - name: Upload APK to Google Play Console
#        uses: r0adkll/upload-google-play@v1
#        with:
#          serviceAccountJsonPlainText: ${{ secrets.PLAY_STORE_JSON }}
#          packageName: "com.tamimibnhossain.financemasterpro"
#          releaseFile: "build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk"
#          track: "internal"
#          releaseName: "Finance Master Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}"
#          whatsNew: "New Version of Finance Master Pro is available. Download Now: [Download the APK](https://drive.google.com/drive/folders/1k51cCnURx-V04zzyrerRMDSo-YCWrKEP?usp=sharing)"
#          releaseNotesFile: "build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk"
#          userFraction: "0.1"
#          releaseStatus: "draft"
#          consoleTimeout: 600000
#
#      - name: Upload APK to Amazon App Store
#        uses: r0adkll/upload-amazon-app-store@v1
#        with:
#                serviceAccountJsonPlainText: ${{ secrets.AMAZON_APP_STORE_JSON }}
#                apkPath: "build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk"
#                packageName: "com.tamimibnhossain.financemasterpro"
#                releaseNotes: "New Version of Finance Master Pro is available. Download Now: [Download the APK](https://drive.google.com/drive/folders/1k51cCnURx-V04zzyrerRMDSo-YCWrKEP?usp=sharing)"
#                releaseStatus: "draft"
#                consoleTimeout: 600000
#
#      - name: Upload APK to Huawei App Gallery
#        uses: r0adkll/upload-huawei-app-gallery@v1
#        with:
#                serviceAccountJsonPlainText: ${{ secrets.HUAWEI_APP_GALLERY_JSON }}
#                apkPath: "build/app/outputs/apk/release/Finance_Master_Pro-${{ steps.increment_version.outputs.new_version }}.${{ steps.date.outputs.date }}.apk"
#                packageName: "com.tamimibnhossain.financemasterpro"
#                releaseNotes: "New Version of Finance Master Pro is available. Download Now: [Download the APK](https://drive.google.com/drive/folders/1k51cCnURx-V04zzyrerRMDSo-YCWrKEP?usp=sharing)"
#                releaseStatus: "draft"
#                consoleTimeout: 600000
#
#      - name: Upload APK to Samsung Galaxy Store
